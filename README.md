# LLVM Bindings

This repository contains generated Ada bindings for LLVM and Clang, along with
the code required to generate them. There is a branch named `llvm-N` for each
supported version of LLVM, where N is the LLVM major version.
